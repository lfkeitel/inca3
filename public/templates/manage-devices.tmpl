{{define "manage-devices"}}
    {{template "header"}}

    {{template "vue-manage-dev-list"}}

    <div class="row">
        <div class="col-md-12">
            <div class="panel-group" id="app">
                <device-item :device="device" v-for="device in devices"></device-item>
            </div>
        </div>
    </div>

    {{template "footer" dict "e" $.e "js" (list "manage-devices")}}
{{end}}